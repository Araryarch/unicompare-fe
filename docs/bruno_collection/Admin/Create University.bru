meta {
  name: Create University
  type: http
  seq: 1
}

post {
  url: {{base_url}}/admin/universities
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id": "test_uni",
    "name": "Test University",
    "sources": ["internal_mock"]
  }
}
